eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    stream:
      bindings:
        input:
          destination: my-pic-comments
          group: my-pic
        output:
          destination: my-pic-comments
          group: my-pic
